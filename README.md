mk_vsa:
utilizza (presuppone) una VM "template" per creare una nuova VSA Commvault. L'utilizzo della voce 'clone_from_template' inibisce o meno le voci sui 'resource'. Vagrant da per scontato le synced_folder, qui appunto disattivate per via dell'utilizzo in vsphere provider
